    let imageX = canvas.width / 2 - 50; 
    let counter = 0;
    const speed = 10;

  
    function draw() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      for count_imma== ++{
        
      }
}
