// gestione sprites aumento contatore per passaggio di sprite
    

let imageX = canvas.width / 2 - 50; 
let counter = 0;
const speed = 10;

const spirets_player1{
  posizione1 : rettangolo1.y,
  posizione1 : rettangolo1.x,
  posizione1 : rettangolo1.speed,
  posizione1 : rettangolo2.y,
  posizione1 : rettangolo2.x,
  posizione1 : rettangolo2.speed

}
  
function disegna() {
  ctx.clearRect(0, 0, canvas.width, canvas.height);
}

